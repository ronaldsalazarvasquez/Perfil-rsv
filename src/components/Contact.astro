---
---

<section id="contact" class="py-20 px-4 relative overflow-hidden">
  <!-- Background Decoration -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-0 right-0 w-96 h-96 bg-[#10b981] opacity-3 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-[#10b981] opacity-3 rounded-full blur-3xl"></div>
  </div>

  <div class="max-w-3xl mx-auto relative z-10">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4 text-[#10b981]" style="font-family: var(--font-main);">
        Trabajemos Juntos
      </h2>
      <p class="text-gray-400 text-lg max-w-2xl mx-auto" style="font-family: var(--font-main);">
        Si tienes un reto de transformación digital o necesitas optimizar tus procesos,
        no dudes en contactarme.
      </p>
    </div>

    <!-- Contact Form -->
    <div class="bg-white/5 rounded-2xl p-8 border border-white/10">
      <h3 class="text-2xl font-bold mb-6 text-[#10b981]" style="font-family: var(--font-handwriting);">
        Envíame un mensaje
      </h3>

      <form
        id="contact-form"
        action="https://api.web3forms.com/submit"
        method="POST"
        class="space-y-6"
      >
        <!-- Web3Forms Access Key -->
        <input type="hidden" name="access_key" value="YOUR_ACCESS_KEY_HERE">

        <!-- Redirect to Email -->
        <input type="hidden" name="email" value="ronaldsalazarvasquez@gmail.com">

        <!-- Honeypot -->
        <input type="checkbox" name="botcheck" class="hidden" style="display: none;">

        <!-- Redirect -->
        <input type="hidden" name="redirect" value="false">

        <!-- Nombre -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-300 mb-2" style="font-family: var(--font-main);">
            Nombre completo
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-lg focus:outline-none focus:border-[#10b981] transition-colors text-white placeholder:text-gray-600"
            style="font-family: var(--font-main);"
            placeholder="Tu nombre"
          />
        </div>

        <!-- Email -->
        <div>
          <label for="user_email" class="block text-sm font-medium text-gray-300 mb-2" style="font-family: var(--font-main);">
            Email
          </label>
          <input
            type="email"
            id="user_email"
            name="user_email"
            required
            class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-lg focus:outline-none focus:border-[#10b981] transition-colors text-white placeholder:text-gray-600"
            style="font-family: var(--font-main);"
            placeholder="tu@email.com"
          />
        </div>

        <!-- Asunto -->
        <div>
          <label for="subject" class="block text-sm font-medium text-gray-300 mb-2" style="font-family: var(--font-main);">
            Asunto
          </label>
          <input
            type="text"
            id="subject"
            name="subject"
            required
            class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-lg focus:outline-none focus:border-[#10b981] transition-colors text-white placeholder:text-gray-600"
            style="font-family: var(--font-main);"
            placeholder="¿Sobre qué quieres hablar?"
          />
        </div>

        <!-- Mensaje -->
        <div>
          <label for="message" class="block text-sm font-medium text-gray-300 mb-2" style="font-family: var(--font-main);">
            Mensaje
          </label>
          <textarea
            id="message"
            name="message"
            required
            rows="5"
            class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-lg focus:outline-none focus:border-[#10b981] transition-colors text-white resize-none placeholder:text-gray-600"
            style="font-family: var(--font-main);"
            placeholder="Cuéntame sobre tu proyecto o idea..."
          ></textarea>
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          class="w-full px-8 py-4 bg-[#10b981] text-white font-semibold rounded-lg hover:bg-[#059669] transition-all duration-300 hover:scale-[1.02] flex items-center justify-center gap-2"
          style="font-family: var(--font-main);"
        >
          <span id="button-text">Enviar mensaje</span>
          <svg id="button-icon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
          <svg id="loading-icon" class="w-5 h-5 animate-spin hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
        </button>

        <!-- Success Message -->
        <div id="success-message" class="hidden p-4 bg-green-500/20 border border-green-500/30 rounded-lg text-green-300 text-sm" style="font-family: var(--font-main);">
          ✅ ¡Mensaje enviado correctamente! Te responderé pronto.
        </div>

        <!-- Error Message -->
        <div id="error-message" class="hidden p-4 bg-red-500/20 border border-red-500/30 rounded-lg text-red-300 text-sm" style="font-family: var(--font-main);">
          ❌ Hubo un error al enviar el mensaje. Por favor, intenta de nuevo.
        </div>
      </form>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const buttonText = document.getElementById('button-text');
  const buttonIcon = document.getElementById('button-icon');
  const loadingIcon = document.getElementById('loading-icon');
  const successMessage = document.getElementById('success-message');
  const errorMessage = document.getElementById('error-message');

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    // Show loading state
    if (buttonText) buttonText.textContent = 'Enviando...';
    buttonIcon?.classList.add('hidden');
    loadingIcon?.classList.remove('hidden');
    successMessage?.classList.add('hidden');
    errorMessage?.classList.add('hidden');

    const formData = new FormData(form);

    try {
      const response = await fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        body: formData
      });

      const data = await response.json();

      if (data.success) {
        successMessage?.classList.remove('hidden');
        form.reset();
      } else {
        errorMessage?.classList.remove('hidden');
      }
    } catch (error) {
      errorMessage?.classList.remove('hidden');
    } finally {
      // Reset button state
      if (buttonText) buttonText.textContent = 'Enviar mensaje';
      buttonIcon?.classList.remove('hidden');
      loadingIcon?.classList.add('hidden');
    }
  });
</script>
